# ===================================================================
# RunPod MusicGen Environment Configuration Template
# ===================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control (it's in .gitignore)

# -------------------------------------------------------------------
# RunPod Connection Configuration
# -------------------------------------------------------------------
# Get these from RunPod console -> Your Pod -> Connect -> SSH
# The hostname will look like: 1a2b3c4d-5e6f-7890-abcd-ef1234567890-pod.ssh.runpod.io
RUNPOD_HOST=your-pod-hostname-here
RUNPOD_USER=root

# -------------------------------------------------------------------  
# AWS S3 Configuration
# -------------------------------------------------------------------
# Get these from AWS IAM console -> Users -> Your User -> Security Credentials
# Make sure your IAM user has S3 permissions for the bucket
MUSICGEN_S3_BUCKET=your-s3-bucket-name-here
AWS_DEFAULT_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-aws-access-key-here
AWS_SECRET_ACCESS_KEY=your-aws-secret-key-here

# -------------------------------------------------------------------
# Optional Configuration
# -------------------------------------------------------------------
# Override default hourly cost for cost estimation (default: based on instance type)
# MUSICGEN_HOURLY_COST=0.526

# Override instance type for cost calculation (default: g4dn.xlarge) 
# INSTANCE_TYPE=g4dn.xlarge

# -------------------------------------------------------------------
# Important Notes
# -------------------------------------------------------------------
# 1. DUAL ENVIRONMENT SETUP:
#    - Local .env file: Used by deployment scripts on your Windows machine
#    - Pod environment: Automatically configured by deployment script via SSH
#
# 2. AUTOMATED DEPLOYMENT:
#    - Deployment script reads this .env file locally
#    - Automatically sets AWS environment variables on the pod
#    - No manual RunPod console configuration required
#    - Worker uses boto3 automatic credential detection
#
# 3. SECURITY:
#    - Never commit .env file to version control
#    - Keep AWS credentials secure
#    - Use IAM permissions to limit S3 access to specific bucket
#
# 4. TESTING SETUP:
#    - Run: python deploy/check_gpu.py (verify GPU works)
#    - Run: python deploy/deploy_to_pod.py (deploy and configure)
#    - Run: python deploy/monitor_logs.py (watch worker logs)
#    - Run: python deploy/deploy_and_monitor.py (all-in-one)